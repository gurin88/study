#include "stdafx.h"
/*
#include <iostream>
using namespace std;

int main()
{
	int case_num = 0;
	int answer = 0;
	int arr[100][100];
	int temp1 = 0;
	int temp2 = 0;
	for (int i = 0 ; i < case_num ; i++)
	{
		cin >> case_num;
		for (int j = 0 ; j < 100 ; j++)
		{
			for (int k = 0; k < 100; k++)
			{
				cin >> arr[j][k];
			}
		}
		for (int l = 0; l < 100; l++)
		{
			int temp = 0;
			for (int m = 0; m < 100; m++)
			{
				temp += arr[l][m];
			}
			if (temp > answer)
			{
				answer = temp;
			}
		}
		for (int l = 0; l < 100; l++)
		{
			int temp = 0;
			for (int m = 0; m < 100; m++)
			{
				temp += arr[m][l];
			}
			if (temp > answer)
			{
				answer = temp;
			}
		}
		for (int l = 0; l < 100; l++)
		{
			int m = 0;
			temp1 += arr[l][m];
			m++;
		}
		for (int l = 99; l >= 0; l--)
		{
			int m = 0;
			temp2 += arr[l][m];
			m++;
		}
	}
	if (temp1 > answer)
	{
		answer = temp1;
	}
	if (temp2 > answer)
	{
		answer = temp2;
	}
	cout << "#" << case_num << " " << answer << endl;

	return 0;
}
*/
