#include<iostream>
using namespace std;
int main()
{
	int N, ans = 0;
	char num;
	cin >> N;
	cin.get();

	for (int i = 0; i < N; i++)
	{
		cin.get(num);
		int temp = num - '0';
		ans += temp;
	}

	cout << ans;
	return 0;
}
