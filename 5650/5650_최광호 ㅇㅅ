#include<iostream>
using namespace std;

// 점수 카운트 함수(처음위치, 현재 위치, 이동방향, 누적 점수)
// 이동방향으로 1칸 이동했을때 구조물, 벽이 있을경우     현재 위치, 이동 방향, 누적 점수    변경 후 재귀
// 이동방향으로 1칸 이동했을때 웜홀이 있을 경우          현재 위치, 이동 방향               변경 후 재귀
// 이동방향으로 1칸 이동했을때 아무것도 없을 경우        현재 위치                          변경 후 재귀
// 이동방향으로 1칸 이동했을때 처음 위치나 블랙홀일경우  누적 점수 반환

int main()
{
	int testcase = 0;
	cin >> testcase;

	for (int a = 0 ; a < testcase ; a++)
	{
		// 배열의 크기 입력
		// 배열 입력

		// 구조물과 웜홀을 제외한 위치 서의 
		// 4가지 방향으로 출발로 했을때 얻을수 있는 점수 체크
		// 더 높은 점수대가 나오면 출력값 최신화
		for (int b = 0; b < length; b++)         // 방향 4가지
		{
			for (int b = 0; b < length; b++)     // x 좌표
			{
				for (int b = 0; b < length; b++) // y 좌표
				{
					// 점수 카운트 함수
					// 더 높은 점수대가 나오면 최신화
				}
			}
		}

		// 점수 출력
	}
}
