#include "stdafx.h"
/*
#include <iostream>

using namespace std;

int cal(int a, int b);

int main()
{
	int a, b;
	int case_num = 10;
	int num;

	for(int i = 0; i < case_num; i++)
	{
		cin >> num;
		cin >> a >> b;
		cout << "#" << num << " " << cal(a, b) << endl;
	}
	return 0;
}

int cal(int a, int b)
{
	if (b == 1)
		return a;
	return (a * cal(a, b-1));
}
*/
