#include "stdafx.h"
/*
#include <iostream>
using namespace std;


int result;
int a[100], b[100];
void find(int n)
{
	if (a[n] == 99 || b[n] == 99)
	{
		result = 1;
		return;
	}
	else
	{
		if (a[n] != -1)
			find(a[n]);
		if (b[n] != -1)
			find(b[n]);
	}
}

int main()
{
	int case_num = 10;
	int num, N;
	for (int q = 0; q < case_num; q++)
	{
		for (int j = 0; j < 100; j++)
		{
			a[j] = { -1 };
			b[j] = { -1 };
		}
		cin >> num >> N;
		result = 0;
		for (int i = 0; i < N; i++)
		{
			int n1, n2;
			cin >> n1 >> n2;
			if (a[n1] == -1)
				a[n1] = n2;
			else 
				b[n1] = n2;
		}
		find(0);
		cout << "#" << num << " " << result << endl;
	}
	return 0;

}

*/
