/*
#include<iostream>
using namespace std;
int main()
{
	int num[8];
	int cnt = 0;;
	for (int i = 0; i < 8; i++)
		cin >> num[i];
	for (int i = 0; i < 7; i++)
		if (num[i + 1] - num[i] > 0)
			cnt++;
		else
			cnt--;
	if (cnt == 7)
		cout << "ascending";
	else if (cnt == -7)
		cout << "descending";
	else
		cout << "mixed";
	return 0;
}*/
