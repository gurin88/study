#include "stdafx.h"
/*
#include<iostream>
#include<list>
using namespace std;

int main()
{
	int num, order_num;
	list <int> key;
	for (int t = 1; t <= 10; t++)
	{
		cin >> num;
		for (int i = 0; i < num; i++)
		{
			int x;
			cin >> x;
			key.push_back(x);
		}
		cin >> order_num;
		for (int j = 0; j < order_num; j++)
		{
			char a;
			cin >> a;
			int x, y;
			list <int> order;
			cin >> x >> y;
			for (int i = 0; i < y; i++)
			{
				int s;
				cin >> s;
				order.push_back(s);
			}
			list <int>::iterator iter;
			iter = key.begin();
			while (x > 0)
			{
				iter++;
				x--;
			}
			key.splice(iter, order);
		}
		cout << "#" << t << " ";
		for (int i = 0; i < 10; i++)
		{
			cout << key.front() << ' ';
			key.pop_front();
		}
		cout << '\n';
		key.clear();
	}
}
*/
