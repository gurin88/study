#include "stdafx.h"
/*
#include <iostream>

using namespace std;

int main()
{
	int length, answer;
	int case_num = 10;
	int num = 1;

	for(int q = 0; q < case_num; q++)
	{
		cin >> length;
		int check[4] = { 0 };
		answer = 1;
		for (int i = 0; i < length; i++)
		{
			char c;
			cin >> c;
			if (c == '(')
				check[0] += 1;
			else if (c == ')')
				check[0] -= 1;
			else if (c == '[')
				check[1] += 1;
			else if (c == ']')
				check[1] -= 1;
			else if (c == '{')
				check[2] += 1;
			else if (c == '}')
				check[2] -= 1;
			else if (c == '<')
				check[3] += 1;
			else if (c == '>')
				check[3] -= 1;
		}
		for (int i = 0; i < 4; i++)
		{
			if (check[i] != 0) answer = 0;
		}
		cout << "#" << num++ << " " << answer << endl;

	}

	return 0;
}
*/
