#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int res;
string s;
void solve(int _num, int _count);

int main()
{
	int TC, count;
	
	cin >> TC;
	
	for (int i = 1; i <= TC; i++) {
		cin >> s;
		cin >> count;

		///////////
	}
	return 0;

}

void solve(int _loc, int _count) {

	int m(0),loc;
	if (_num == _count) {
		res = max(res, stoi(s));
	}
	for (int i = 0; i <_count; i++) {
		
	}
}
