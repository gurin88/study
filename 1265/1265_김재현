#include "stdafx.h"
/*
#include <iostream>

using namespace std;

int N;
char X[500];
char Y[500];
double dp[500][500] = { 0 };

int main()
{
	cout << fixed;
	cout.precision(2);
	int test_case;
	cin >> test_case;
	for (int T = 1; T <= test_case; T++)
	{
		cin >> N;
		for (int i = 0; i < N; i++)
		{
			cin >> X[i];
		}
		for (int i = 0; i < N; i++)
		{
			cin >> Y[i];
		}

		for (int i = 0; i < N; i++)
		{
			for (int j = 0; j < N; j++)
			{
				if (X[i + 1] == Y[j + 1])
				{
					dp[i + 1][j + 1] = dp[i][j] + 1;
				}
				else if (dp[i][j + 1] > dp[i + 1][j])
				{
					dp[i + 1][j + 1] = dp[i][j + 1];
				}
				else
					dp[i + 1][j + 1] = dp[i + 1][j];
			}
		}

		double res = double(dp[N][N] * 100 / N);
		cout << "#" << T << " ";
		cout << res << endl;
	}
	return 0;
}*/
